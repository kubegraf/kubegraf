apiVersion: v1
kind: Service
metadata:
  name: webapp-test-service
  labels:
    app: webapp-test
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: webapp-test
    tier: frontend

